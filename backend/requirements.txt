# Production dependencies — keep in sync with pyproject.toml
# This file is used by AWS SAM for Lambda package builds.
# To regenerate: poetry export -f requirements.txt --without-hashes -o requirements.txt
fastapi>=0.128.0
uvicorn[standard]>=0.40.0
python-dotenv>=1.0.1
supabase>=2.10.0
pydantic[email]>=2.10.0
pydantic-settings>=2.6.0
python-multipart>=0.0.22
pandas>=2.2.3
mangum>=0.21.0
