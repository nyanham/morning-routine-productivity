name: Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

env:
  NODE_VERSION: "22"
  PYTHON_VERSION: "3.11"

jobs:
  # ==========================================
  # Deploy Frontend to Vercel
  # ==========================================
  deploy-frontend:
    name: Deploy Frontend
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: "npm"
          cache-dependency-path: frontend/package-lock.json

      - name: Install Vercel CLI
        run: npm install --global vercel@latest

      - name: Pull Vercel Environment Information
        run: vercel pull --yes --environment=production --token=${{ secrets.VERCEL_TOKEN }}

      - name: Build Project Artifacts
        run: vercel build --prod --token=${{ secrets.VERCEL_TOKEN }}

      - name: Deploy Project Artifacts to Vercel
        run: vercel deploy --prebuilt --prod --token=${{ secrets.VERCEL_TOKEN }}

  # ==========================================
  # Deploy Backend to AWS Lambda (via SAM)
  # ==========================================
  # Uncomment this job when you're ready to deploy the backend.
  # Required GitHub Secrets:
  #   - AWS_ACCESS_KEY_ID
  #   - AWS_SECRET_ACCESS_KEY
  #   - SUPABASE_URL
  #   - SUPABASE_KEY
  #   - CORS_ORIGINS (optional, exact-match origins)
  #   - CORS_ORIGIN_REGEX (optional, regex for wildcard subdomains)
  #
  # deploy-backend:
  #   name: Deploy Backend to AWS Lambda
  #   runs-on: ubuntu-latest
  #   needs: deploy-frontend
  #   defaults:
  #     run:
  #       working-directory: backend
  #
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v6
  #
  #     - name: Setup Python
  #       uses: actions/setup-python@v6
  #       with:
  #         python-version: ${{ env.PYTHON_VERSION }}
  #
  #     - name: Install AWS SAM CLI
  #       uses: aws-actions/setup-sam@v2
  #       with:
  #         use-installer: true
  #
  #     - name: Configure AWS credentials
  #       uses: aws-actions/configure-aws-credentials@v4
  #       with:
  #         aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
  #         aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
  #         aws-region: us-east-1
  #
  #     - name: SAM Build
  #       run: sam build
  #
  #     - name: SAM Deploy
  #       run: |
  #         sam deploy --no-confirm-changeset --no-fail-on-empty-changeset \
  #           --stack-name morning-routine-api-prod \
  #           --parameter-overrides \
  #             Environment=production \
  #             SupabaseUrl=${{ secrets.SUPABASE_URL }} \
  #             SupabaseKey=${{ secrets.SUPABASE_KEY }} \
  #             CorsOrigins="${{ secrets.CORS_ORIGINS || 'https://your-production-domain.vercel.app' }}" \
  #             CorsOriginRegex="${{ secrets.CORS_ORIGIN_REGEX || '' }}"
